import { Container, Posts } from "@components/index";
import React from "react";

const Home: React.FC = () => {
    return (
        <Container>
            <Posts />
        </Container>
    );
};

export default Home;
